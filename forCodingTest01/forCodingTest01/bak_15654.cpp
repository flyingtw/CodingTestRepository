#include <bits/stdc++.h>

using namespace std;

int M, N;
vector<int>v;
bool check[10];



int main() {
	cin >> N >> M;
	for (int i = 0; i < N; i++) {
		int a; cin >> a;
		v.push_back(a);
	}
	sort(v.begin(), v.end());
	



	return 0;
}
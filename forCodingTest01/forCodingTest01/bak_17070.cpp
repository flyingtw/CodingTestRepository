#include <bits/stdc++.h>
#define endl "\n"
using namespace std;
void init();

//(0,0)-(0,1) 파이프 시작
// 가로 : (->), (
// 세로 : 아래로 한칸, 오른쪽 45도
// 대각선 : 


int N;
int arr[20][20];


int main() {
	init();
	cin >> N;
	/*for (int i = 0; i < N; i++) {
		for (int j = 0; j < N; j++) {

		}
	}*/

	return 0;
}
void init() {
	cin.tie(NULL);
	cout.tie(NULL);
	ios::sync_with_stdio(false);
}